import React from 'react';
import PropTypes from 'prop-types';

const Contact =() => {
    return (
        <div>
            <h2>Contact us</h2>
            <p>we are one coll way</p>
        </div>
    );
};

Contact.propTypes = {
    
};

export default Contact;