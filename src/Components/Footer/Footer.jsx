import React from 'react';
import PropTypes from 'prop-types';

const Footer = props => {
    return (
        <div>
            <h2 className='text-2xl font-bold'>Thank you for visiting our website</h2>
        </div>
    );
};

Footer.propTypes = {
    
};

export default Footer;